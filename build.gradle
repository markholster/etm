ext {
    version_elasticsearch = "6.3.1"
    dep_junit = "junit:junit:4.12"
}

allprojects {
    apply plugin: "eclipse"
    apply plugin: "idea"
    version "3.3.0-SNAPSHOT"
    group "com.jecstar.etm"

    repositories {
        mavenCentral()
    }
}
