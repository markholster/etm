ext {
    dep_junit = "junit:junit:4.12"
}

allprojects {
    apply plugin: "eclipse"
    apply plugin: "idea"
    version "3.1.1"
    group "com.jecstar.etm"

    repositories {
        mavenCentral()
    }
}
