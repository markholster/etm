plugins {
    id 'java'
}

dependencies {
    implementation project(":etm-server-core"), project(":etm-public:etm-domain"), dep_metricsCore, dep_disruptor, dep_elasticsearchClient, dep_jacksonDatabind
}